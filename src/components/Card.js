import React from 'react';
import CardText from './CardText';
import './css/Card.css';

const Card = (props) => {
  return (
    <div className="Card">
      <CardText>
      </CartText>
    </div>
  );
}

export default Card;

